# 工作者日誌（Work Log）

此專案為單頁個人簡介網站（靜態網頁：HTML / CSS / JavaScript）。

## 專案檔案

- `index.html`：個人簡介網站主頁面
- `about.html`：**關於我／自傳**獨立頁面（導覽標籤為「關於我」）
- `todo-app.html`：**作品 B** 待辦清單（LocalStorage、篩選、排序）
- `data-viz.html`：**作品 C** 資料視覺化（Fetch、Chart.js、載入／錯誤／空狀態）
- `games.html`：Python遊戲展示頁面（包含嵌入式遊戲代碼）
- `styles.css`：全站樣式檔案
- `script.js`：網站互動功能
- `minesweeper.py`、`tic_tac_toe.py`、`brick_breaker.py`：原始Python遊戲檔案（用於開發維護，實際運行時使用嵌入式代碼）
- `unity-game/index.html`：**Unity 2D 獨立作品頁**（與 Python 小遊戲集分開）；`unity-2d.html` 會導向此頁

## 記錄規範（建議）

- 每次工作都新增一筆「日期」條目
- 內容至少包含：做了什麼、遇到什麼問題、如何解決、下一步
- 若有修改功能或版面，請一併註記影響範圍（例如：哪個區塊/哪個檔案）

## 日誌模板

```text
### YYYY-MM-DD

- **目標**：
- **完成事項**：
- **問題/風險**：
- **解法/備註**：
- **下一步**：
```

---

### 2026-03-04

- **目標**：建立單頁個人簡介網站（傳統靜態網頁，只有一頁）。
- **完成事項**：
  - 建立 `index.html`：導覽列、Hero、關於我、技能、作品、經歷、聯絡區塊、頁尾。
  - 建立 `styles.css`：現代感視覺、RWD、卡片樣式、按鈕、排版與動畫樣式。
  - 建立 `script.js`：
    - 深色模式（自動/淺色/深色循環，並用 `localStorage` 記住）
    - 手機漢堡選單開合（含 ESC 關閉、點擊外部關閉）
    - 滾動導覽高亮（ScrollSpy）
    - 區塊滑入顯示（IntersectionObserver）
    - 複製 Email（Clipboard API，含 fallback）
    - 回到頂部按鈕、頁尾年份自動更新
- **問題/風險**：
  - Email 目前是範例值 `yourname@example.com`，需要換成真實資料。
  - 作品連結目前為 placeholder（`#`），需要補上 Demo/GitHub。
- **解法/備註**：
  - 直接用瀏覽器開啟 `index.html` 即可預覽（不需任何框架或建置工具）。
- **下一步**：
  - 替換個人資料（姓名、Email、社群連結、作品內容）。

### 2026-03-18

- **目標**：加入遊戲區塊作為程式能力的成果展示，包含用Python完成的小遊戲。
- **完成事項**：
  - 在 `index.html` 的作品區塊中新增「Python小遊戲集」項目卡片。
  - 建立 `games.html` 頁面，使用Pyodide在瀏覽器中運行Python遊戲。
  - 實作三個Python遊戲：
    - `minesweeper.py`：踩地雷遊戲，包含地雷生成、點擊揭示、勝利/失敗判斷。
    - `tic_tac_toe.py`：井字遊戲，雙人對戰，先連成一線獲勝。
    - `brick_breaker.py`：打磚塊遊戲，使用Canvas繪圖，包含球運動、磚塊碰撞、計分系統。
  - 在 `styles.css` 中新增遊戲相關樣式。
  - 更新導覽連結以支援新頁面。
- **問題/風險**：
  - Pyodide載入時間可能較長，需要優化載入體驗。
  - 遊戲邏輯在瀏覽器環境中運行，可能有相容性問題。
- **解法/備註**：
  - 使用CDN載入Pyodide，確保遊戲能正常運行。
  - 每個遊戲都有重新開始功能，提升使用者體驗。
- **下一步**：
  - 測試遊戲在不同瀏覽器中的相容性。
  - 考慮添加更多遊戲或改進現有遊戲的UI。

### 2026-03-18 (修復遊戲載入問題)

- **目標**：修復Pyodide遊戲載入和互動問題。
- **完成事項**：
  - 重構遊戲架構，使用JavaScript處理用戶事件，Python處理遊戲邏輯。
  - 修復minesweeper.py：使用類別設計，添加點擊處理函數。
  - 修復tic_tac_toe.py：實現完整的遊戲邏輯和狀態管理。
  - 重寫brick_breaker.py：簡化動畫循環，添加開始按鈕控制。
  - 在games.html中添加對應的JavaScript事件處理函數。
  - 改善錯誤處理和載入狀態顯示。
- **問題/風險**：
  - Pyodide DOM操作複雜，需要仔細處理JavaScript-Python互動。
  - 動畫遊戲需要適當的幀率控制。
- **解法/備註**：
  - 使用pyodide.runPython()執行Python函數並獲取返回值。
  - 將事件監聽器放在JavaScript中，調用Python函數處理邏輯。
  - 添加載入狀態和錯誤處理，提升用戶體驗。
- **下一步**：
  - 在瀏覽器中測試遊戲功能。
  - 優化遊戲性能和響應性。

### 2026-03-18 (修復遊戲載入和互動問題)

- **目標**：修復遊戲無法正常載入和運行的問題。
- **完成事項**：
  - 修復井字遊戲的狀態顯示邏輯，添加`get_game_status()`函數供JavaScript調用。
  - 改善JavaScript錯誤處理，提供更詳細的錯誤信息和調試日誌。
  - 添加遊戲重新開始功能，支持重置遊戲狀態而不重新載入整個Pyodide。
  - 修復Pyodide與JavaScript的互動問題，避免直接在JS中調用Python對象方法。
  - 為每個遊戲添加`restart_game()`函數。
- **問題/風險**：
  - Pyodide載入可能因網路問題失敗。
  - JavaScript調用Python對象方法的複雜性。
  - 遊戲狀態管理在重新開始時的問題。
- **解法/備註**：
  - 添加詳細的console.log調試信息。
  - 使用Python全局函數而不是直接操作Python對象。
  - 實現遊戲特定的重新開始邏輯。
  - 改善用戶體驗，避免不必要的alert彈窗。
- **下一步**：
  - 測試所有遊戲的完整功能。
  - 考慮添加遊戲說明和操作提示。

### 2026-03-18 (將遊戲分離到單獨頁面)

- **目標**：為每個遊戲建立獨立頁面，避免同時開啟多個遊戲時的狀態衝突和bug。
- **完成事項**：
  - 建立 `minesweeper.html`：專屬踩地雷遊戲頁面，內嵌Python代碼。
  - 建立 `tic-tac-toe.html`：專屬井字遊戲頁面，內嵌Python代碼。
  - 建立 `brick-breaker.html`：專屬打磚塊遊戲頁面，內嵌Python代碼。
  - 重構 `games.html` 為遊戲選單頁面，移除內嵌遊戲代碼，改為連結到各遊戲頁面。
  - 在 `styles.css` 中新增遊戲卡片圖標和懸停效果。
  - 更新所有頁面的導覽連結和返回按鈕。
- **問題/風險**：
  - 多個遊戲同時運行時可能造成Pyodide實例衝突。
  - 遊戲狀態互相干擾，變數名稱衝突。
  - 記憶體洩漏和性能問題。
- **解法/備註**：
  - 每個遊戲頁面都有獨立的Pyodide實例和HTML結構。
  - 遊戲選單頁面簡潔明瞭，提供遊戲說明和連結。
  - 保持原始Python檔案作為開發維護用。
  - 新增遊戲圖標和懸停動畫，提升視覺效果。
- **下一步**：
  - 測試各遊戲頁面的獨立運行。
  - 確認沒有跨頁面干擾。
  - 考慮添加遊戲難度選項或更多遊戲功能。

---

### 2026-03-04

- **目標**：強化互動與視覺呈現，加入進階效果並確保無障礙與效能。
- **完成事項**：
  - 增加 canvas 漂浮背景特效（`fx-canvas`）和自訂游標光暈。
  - 實作滾動進度條元件（`.scroll-progress`）以視覺提示頁面長度。
  - 修正並補充 reveal 動畫、ScrollSpy 與漢堡選單關閉行為。
  - 優化深色/淺色主題切換、加入 localStorage 儲存機制與自動偵測。
  - 加入電子信箱複製按鈕、RWD 漢堡選單、回到頂端按鈕、年份自動更新等小功能。
- **問題/風險**：
  - 部分動畫在降低動態模式下應關閉。
  - Canvas 與光暈特效可能影響效能，已添加媒體查詢檢測。
- **解法/備註**：
  - 使用 `IntersectionObserver` 實作 reveal 與 ScrollSpy，下方的 fallback 提升相容性。
  - 若使用者偏好 reduced-motion，自動停用浮動與光暈效果。
- **下一步**：
  - 填入實際作品連結與個人資訊。
  - 測試不同裝置的效能和無障礙情況，必要時微調動畫。

### 2026-03-18 (更新踩地雷遊戲使用mid資料夾的進階模型)

- **目標**：將踩地雷遊戲升級為使用mid資料夾內更完整的遊戲模型。
- **完成事項**：
  - 將 `mid/game_logic.py` 的完整遊戲邏輯整合到 `minesweeper.html` 中。
  - 新增難度選擇功能（簡單、普通、困難）。
  - 新增右鍵插旗功能，用於標記可疑地雷。
  - 實作「首點不死」機制，確保第一次點擊不會踩到地雷。
  - 新增遊戲狀態顯示，包含剩餘地雷數量和勝利/失敗提示。
  - 改善遊戲邏輯，使用BFS演算法進行區域擴展。
  - 更新CSS樣式，支援響應式設計和更好的視覺效果。
- **問題/風險**：
  - 從Flask後端邏輯轉換為Pyodide前端邏輯的複雜性。
  - 確保右鍵點擊事件正確處理而不觸發瀏覽器選單。
  - 不同難度下網格佈局的自適應問題。
- **解法/備註**：
  - 保留Cell和Game類的核心邏輯，移除Flask相關代碼。
  - 使用 `oncontextmenu` 事件處理右鍵點擊，並阻止預設行為。
  - 動態設定CSS grid佈局以適應不同難度。
  - 新增遊戲狀態追蹤和即時更新。
- **下一步**：
  - 測試所有難度的遊戲功能。
  - 確認右鍵插旗功能正常運作。
  - 考慮添加遊戲統計功能（如遊玩時間、勝率等）。

---

### 2026-03-18 (井字遊戲改用 Tic-Tac-Toe 資料夾模型)

- **目標**：將井字遊戲改為使用 `Tic-Tac-Toe/` 資料夾內的進階 JavaScript 遊戲模型。
- **完成事項**：
  - 移除 `tic-tac-toe.html` 中的 Pyodide 依賴及 Python 代碼。
  - 整合 `Tic-Tac-Toe/script.js` 的完整遊戲邏輯（雙人模式、單人對電腦模式、Minimax 演算法）。
  - 整合 `Tic-Tac-Toe/style.css` 的樣式設計（深色主題、響應式設計、動畫效果）。
  - 新增遊戲模式選擇（雙人遊玩 / 單人對電腦）。
  - 新增符號選擇功能（X 先手 / O 後手）。
  - 保留網站統一的頁面結構與導航功能。
  - 更新遊戲說明，包含新功能的介紹。
- **問題/風險**：
  - 從 Pyodide Python 邏輯轉換為純 JavaScript 邏輯的整合複雜性。
  - 確保樣式與網站整體設計的一致性。
  - 電腦 AI 使用 Minimax 演算法的效能考量。
- **解法/備註**：
  - 直接內嵌 JavaScript 代碼和 CSS 樣式到 `tic-tac-toe.html` 中。
  - 使用類別前綴 `tic-tac-toe-` 避免與網站樣式衝突。
  - 電腦 AI 採用零總和 Minimax 演算法，確保最佳策略。
  - 遊戲支援無障礙設計（ARIA 標籤、鍵盤導航）。
- **下一步**：
  - 測試雙人模式和電腦對戰模式的完整功能。
  - 確認在不同裝置上的響應式表現。
  - 考慮添加遊戲統計功能（如勝率記錄）。

---

### 2026-03-04（Unity 作品獨立）

- **目標**：將 Unity 遊戲從 Python 小遊戲集中獨立為單一作品展示。
- **完成事項**：
  - 新增 `unity-game/index.html`、`unity-game/README.md` 作為 Unity 專屬介紹頁（資源路徑使用 `../styles.css`、`../script.js`）。
  - `games.html` 移除 Unity 卡片，並在說明中連結至 Unity 獨立頁。
  - `unity-2d.html` 改為自動導向 `unity-game/index.html`，保留舊書籤可用。
  - `index.html` 作品區新增「Unity 2D 冒險遊戲」卡片；`script.js` 補上 `unity`、`games` 的 Modal 資料，且 Demo 連結僅在外部網址時另開分頁。
  - 修復 `index.html` 頁首損壞的 `header-actions` 結構。
- **下一步**：若有 WebGL 建置，可嵌入 `unity-game/` 或加上 itch.io／下載連結。

---

### 作品 B／C 完成

- 新增 `todo-app.html`：待辦清單（新增／完成／雙擊編輯／刪除、篩選、排序、LocalStorage、清除已完成）。
- 新增 `data-viz.html`：JSONPlaceholder + Chart.js 長條圖、載入／錯誤重試／空資料示範。
- `index.html` 與 `script.js` 已更新作品 B、C 的文案、標籤與 Demo 連結。

### 自傳頁面（關於我）

- 新增 `about.html`：獨立「自傳」內容，導覽列以 **關於我** 為標籤連至此頁（`aria-current="page"`）。
- `index.html`：導覽「關於我」、Hero「認識我」改連 `about.html`；首頁「關於我」區塊加連結至自傳頁。
- 全站子頁（`games.html`、`minesweeper.html`、`tic-tac-toe.html`、`brick-breaker.html`、`unity-game/index.html`）的「關於我」改為 `about.html`（或 `../about.html`）。

---

